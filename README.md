# federated_fake_news_detection
This project implements a Federated Learning (FL) framework for fake news detection using a BERT-based model.  Unlike traditional centralized systems, this approach ensures:   Data privacy (no raw data sharing)  ,Distributed training across clients, Handling of non-IID data,  Reduced communication overhead
